The prior response failed validation for this reason:

{{ERROR}}

Rewrite the response as one corrected JSON object.  Return no code fences or prose outside the JSON object.  Keep one claim, keep `legal_theory` short, and keep `elements` and `defenses` as short phrases.
