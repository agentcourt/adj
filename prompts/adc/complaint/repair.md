The prior complaint draft failed validation for this reason:

{{ERROR}}

Rewrite the complaint in Markdown.  Keep the required headings exactly.  Use ordinary Markdown links in the exact form `[label](path)`, copying every listed `reference_path` exactly.  Return the complaint without analysis or code fences.
